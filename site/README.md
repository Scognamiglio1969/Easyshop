# Easyshop landing page

Public product page for Easyshop 0.1 Alpha. It includes the interactive product tour, feature overview, founder story, GitHub links and the macOS DMG download.

```bash
npm install
npm run dev
npm test
```

The site is deployed with OpenAI Sites on Cloudflare infrastructure. It does not use Vercel, accounts, analytics or cookies.
